apiVersion: v1
kind: Service
metadata:
  name: tasks-agent
  namespace: tasks-manager
  labels:
    app: tasks-agent
spec:
  type: ClusterIP
  selector:
    app: tasks-agent
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
