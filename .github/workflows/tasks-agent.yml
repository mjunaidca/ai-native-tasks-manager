name: tasks-agent

on:
  push:
    branches: [main]
    paths:
      - 'services/tasks-agent/**'
      - '.github/workflows/tasks-agent.yml'
    tags:
      - 'tasks-agent-v*'
  pull_request:
    paths:
      - 'services/tasks-agent/**'
      - '.github/workflows/tasks-agent.yml'

concurrency:
  group: tasks-agent-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE: ghcr.io/${{ github.repository }}/tasks-agent

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute tags
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=short,prefix=
            type=match,pattern=tasks-agent-v(.*),group=1

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: services/tasks-agent
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=tasks-agent
          cache-to: type=gha,scope=tasks-agent,mode=max
